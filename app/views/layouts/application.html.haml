<!DOCTYPE html>
%html
  %head
    %title FastSchemes
    -if current_user.nil?
      = stylesheet_link_tag "base_theme"
    -else
      -if current_user.theme == "Темная тема"
        = stylesheet_link_tag "dark_theme"
      -else        
        = stylesheet_link_tag "base_theme"
      =csrf_meta_tags
      

  = render 'layouts/header'
  %section.main-content
    = yield
=javascript_include_tag 'application'
= yield :scripts
    